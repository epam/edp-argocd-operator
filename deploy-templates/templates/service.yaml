apiVersion: v1
kind: Service
metadata:
  name: {{ include "edp-argocd-operator.fullname" . }}
  labels:
    {{- include "edp-argocd-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "edp-argocd-operator.selectorLabels" . | nindent 4 }}
